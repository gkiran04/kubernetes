---
    apiVersion: v1
    kind: Service
    metadata:
      name: springpetclinic
    spec:
      selector:
        app: springpetclinic
        ver: 1.0.0
      type: LoadBalancer
      ports:
        - port: 8080